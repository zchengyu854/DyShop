# 客户端收货地址模块 — 手测与自动化记录（2026-08-05）

> 任务：T12 · 用例基线：docs/ch06/spec.md §6 验收标准

## 1. 环境前置

- 后端：`mvnw -pl dyshop-api spring-boot:run`（:8081）
- 前端：`npm run dev`（:5173）
- 数据库：docker mysql-dev（地址数据已验证 UTF-8 正常）
- 账号：buyer01 / test123456

## 2. 自动化验证（Playwright，35 断言全 PASS）

```bash
cd frontend && node addr-e2e.cjs   # 备份：/var/folders/.../T/opencode/addr-e2e.cjs
```

| 用例 | 结果 |
| --- | --- |
| 未登录访问 `/addresses`（含 `/addresses` → `/user/addresses` 重定向）→ 路由守卫跳登录 | ✅ |
| 空态：邮筒插画 +「还没有收货地址」+ 计数 0/20 | ✅ |
| 空态「新增地址」打开弹层；省/市/区默认禁用、选省启市、三级联动 | ✅ |
| 非法手机号（12345）保存拒绝 + 显式错误 | ✅ |
| 新增保存：toast「地址已添加」、弹层关闭、计数 1/20 | ✅ |
| **首条自动默认**：默认标签、手机号脱敏 `138****8000`、默认卡无「设为默认」 | ✅ |
| 新增第二条（非默认）→ 出现「设为默认」按钮、计数 2/20 | ✅ |
| **设默认**：toast「已设为默认地址」+ 默认标签互斥置顶（FLIP 位移动画） | ✅ |
| **编辑默认地址**：设默认 switch 禁用 + 提示「默认地址不可取消」；改名保存 → toast「地址已更新」 | ✅ |
| **删除默认地址**：确认弹窗追加「删除后将自动把最近添加的地址设为默认」；确认后剩余地址自动默认 | ✅ |
| 删除非默认地址：无顺延说明；取消保留；确认删除 | ✅ |
| **上限 20 条**：UI 计数 20/20（橙色 count-full）、虚线新增卡消失；第 21 条 API 拒绝 body `code:400` | ✅ |
| 全部删除 → 恢复空态 | ✅ |

## 3. 后端行为补充确认（curl）

- 上限错误载体：`BizException(PARAM_ERROR)` → **HTTP 200 + body `{code:400, message:"地址数量已达上限（20 条）"}`**（非 HTTP 404/500；前端 request.js 按 body code 判定）
- 删除默认地址顺延：`list` 排序 = is_default 倒序 + create_time 倒序，与顺延规则一致

## 4. 与验收标准的对照

| spec §6 验收 | 结论 |
| --- | --- |
| 后端编译通过；curl 覆盖列表/新增/首条默认/上限/手机号/越权 404/编辑/设默认互斥/删除默认顺延/未登录 401 | ✅（T5 已测，本次复核上限行为） |
| 前端 `npm run build` 通过 | ✅（T11） |
| 浏览器手测全流程 | ✅ 本文件 §2 |

## 5. 备注

- 测试数据已清理（buyer01 地址归零）。测试脚本留在 opencode 临时目录，未入库。
- 编辑默认地址时 name 变更为「测试二号-编辑」后删除的流程覆盖了「编辑后再删默认」的组合场景。